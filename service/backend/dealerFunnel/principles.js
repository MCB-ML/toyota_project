// 딜러 계약퍼널 — 숫자를 말로 옮길 때 지키는 판단 원칙 (요구사항정의서 4장).
//
// **AI가 문구를 쓰는 자리가 둘이라 여기 한 곳에 둔다.**
//   narrate.js   이상현상 원인 해석 — 이 원칙 + 역할·출력형식을 합쳐 시스템 프롬프트로
//   htmlEdit.js  HTML 문서 편집 — 데이터 블록에 함께 실어 보낸다
//
// 복사해 두 벌로 만들면 한쪽만 고쳐져 갈린다. 같은 데이터를 놓고 화면의 AI 해석과
// 문서 안의 문구가 서로 다른 기준으로 쓰이면, 읽는 사람은 둘 중 뭘 믿어야 할지 모른다.
//
// 정의서 7장이 "4장 프롬프트 초안은 실제 운영하면서 계속 다듬어질 것을 전제로, 담당자가
// 지속적으로 수정할 수 있는 구조"를 요청했다 — 그래서 코드 곳곳이 아니라 이 파일 하나다.
//
// 7-1·7-2는 정의서에 없다. 계약 지표를 붙이고 실측하다 나온 것이라 번호를 이어 붙였다:
//   7-1  시승→계약이 147%로 나왔다 (2026-08-11). 각 단계를 자기 기준일로 따로 센 값의 비다.
//   7-2  계약의 19.5%가 채널 미상이다. 채널별 합이 총계보다 작다.

/**
 * 원칙 본문. 문장을 고칠 때는 두 소비처(narrate·htmlEdit)에 같이 적용된다는 걸 염두에 둔다.
 *
 * forecast_used·small_sample·over_100은 탐지 결과(anomaly.js)가 붙이는 플래그 이름이다.
 * 해석 쪽은 그 JSON을 직접 받고, HTML 편집 쪽은 문서의 이상현상 표에서 같은 표시를 본다.
 */
export const INTERPRETATION_PRINCIPLES = `1. 전환율과 절대치를 항상 함께 본다. 절대치가 줄면서 전환율만 오르는 경우(착시)와, 절대치가 늘면서
   전환율이 내리는 경우(반드시 나쁜 신호는 아님)를 구분해 설명한다.
2. 비교 기준 시점을 항상 명시한다. "증가했다/감소했다"만 쓰지 말고 "6월 대비 7월"처럼 무엇과 비교했는지 밝힌다.
3. 원인은 세 축으로 좁힌다 — 시점(언제 꺾였나) · 딜러(특정 딜러인가 전사인가) · 세부모델/트림.
   주어진 데이터에 그 축이 없으면 "이 데이터로는 구분할 수 없다"고 쓴다.
4. 가설은 데이터로 검증한다. 타이밍이 비슷하다는 이유만으로 인과를 단정하지 않는다.
   검증되지 않은 것은 "가능성" 수준으로만 쓰고 확정적으로 서술하지 않는다.
5. 부분월 실적을 전월과 직접 비교하지 않는다. forecast_used가 true인 항목은 이미 예상 최종치로
   환산해 비교한 것이므로 그 사실을 밝힌다.
6. small_sample이 true인 항목은 결론의 근거로 쓰지 않는다. 언급하더라도 "표본이 작아 참고용"이라고 명시한다.
7. "관심모델없음"은 기회가 없다는 뜻이 아니라 관심 차종이 기록되지 않았다는 뜻이다.
7-1. 전환율에 over_100이 true면 **같은 건이 두 단계를 순서대로 지난 비율이 아니다.** 각 단계를
   자기 기준일(활동일자/기회생성일자/계약일자)로 따로 센 값의 비이므로 100%를 넘을 수 있다.
   시승 없이 바로 계약된 건, 시승은 지난달이고 계약은 이번 달인 건이 섞인다.
   "전환율이 급등했다"로 쓰지 말고 그 달에 무엇이 늘었는지를 절대치로 설명한다.
7-2. 채널 귀속 실패가 있는 지표는 채널별 합이 총계보다 작다. 채널 비중을 100% 기준으로
   말하지 말고, 몇 %가 채널 미상인지 함께 밝힌다.
8. 데이터에 없는 사실을 지어내지 않는다. 외부 요인(신차 출시, 프로모션, 재고 상황)은 사용자가 알려준
   경우에만 반영하고, 모르면 "현장 확인이 필요하다"고 쓴다.`

/** HTML 편집 챗봇에 실어 보낼 블록. 문서를 고치는 일과 문구를 쓰는 일이 섞이므로 범위를 못박는다. */
export const PRINCIPLES_BLOCK = `[문구를 쓸 때 지킬 판단 원칙 — 요구사항정의서 4장]
아래는 이 문서의 숫자를 말로 옮길 때 지켜야 하는 기준입니다. 요약·해설·제목처럼 **새로 쓰는 문구**에
적용합니다. 레이아웃·색·차트 종류를 바꾸는 일에는 해당하지 않습니다.

${INTERPRETATION_PRINCIPLES}`
